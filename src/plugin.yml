name: ServerBackup
author: Seblii
version: 2.6.1
api-version: 1.18

main: de.sebli.serverbackup.ServerBackup

commands:
  backup:
    aliases: [serverbackup]
    permission: backup.admin

permissions:
  backup.admin:
    description: Permission for all backup commands